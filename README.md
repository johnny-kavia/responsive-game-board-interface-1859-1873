# responsive-game-board-interface-1859-1873

## Containers

### tic_tac_toe_frontend
React frontend for a responsive Tic Tac Toe game (UI + game logic in-browser).
Run it from `tic_tac_toe_frontend/` with `npm install` then `npm start` (default port 3000).